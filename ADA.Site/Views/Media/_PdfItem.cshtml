@model ADA.Domain.Media.Medium
    

<div class="content">
    <span class="font-weight-bold">@Html.DisplayFor(modelItem => modelItem.Titre)</span>
    @if ((bool)ViewData["isFond"])
    {
        Html.RenderPartial("~/Views/Fonds/_Informations.cshtml", Model);
    }
    @{ Html.RenderPartial("~/Views/Media/_MediumTags.cshtml", Model); }
</div>
<div class="flex-right">
    <a href="@Url.HttpRouteUrl("InlineContentApi", new { controller = "Media", action= "inline-content", id = Model.Id, niceUrl = Model.FileName })" target="_blank">
        @if (!String.IsNullOrEmpty(Model.RepertoireNomThumbnail))
        {
            <img src="@Url.Action("Thumbnail", "Media", new { id = Model.Id })" />
        }
        else
        {
            <i class="fas fa-file-pdf fa-lg"></i>
        }

        </a>
</div>
